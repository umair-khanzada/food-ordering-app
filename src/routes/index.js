// implement router switch here and also map over route config to map all routes with authguard

export default function Route () {
    return (
        <div>
            This is router Mapper component
        </div>
    )
}