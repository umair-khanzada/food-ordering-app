// import { SAVE_RESTAURANT } from '../../redux/ActionTypes';
// const initialState = { data: [] };

// export const saveRestaurant = (state = { ...initialState }, action) => {
//   console.log(action);
//   switch (action.type) {
//     case SAVE_RESTAURANT:
//       // eslint-disable-next-line no-case-declarations
//       const { results } = action.payload;
//       return { ...state, results };
//     default:
//       return state;
//   }
// };
