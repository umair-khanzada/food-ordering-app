export const ForgetPasswordReducer = (state = {}, action) => {
  const { type } = action;
  switch (type) {
    case 'ForgetPassword': // must Proper const here
    default:
      return state;
  }
};
